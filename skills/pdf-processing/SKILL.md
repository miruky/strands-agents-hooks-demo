---
name: pdf-processing
description: PDFファイルからテキストとテーブルを抽出する
allowed-tools: file_read shell
---
# PDF処理

PDFからコンテンツを抽出する際は以下の手順に従ってください：

1. `shell` で `scripts/extract.py` を実行
2. `file_read` で出力を確認
3. 抽出内容を要約して報告
